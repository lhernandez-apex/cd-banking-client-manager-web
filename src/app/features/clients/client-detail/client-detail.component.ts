import { Component } from '@angular/core';

/**
 * Placeholder for the client detail view component.
 * Full implementation delivered in phase 7 (ng-detail).
 */
@Component({
  selector: 'app-client-detail',
  standalone: true,
  template: `<p>Client detail — coming in phase 7</p>`
})
export class ClientDetailComponent {}
