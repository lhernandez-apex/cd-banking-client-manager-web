import { Component } from '@angular/core';

/**
 * Placeholder for the client create/edit form component.
 * Full implementation delivered in phase 6 (ng-form).
 * Handles both /clients/new and /clients/:id/edit routes.
 */
@Component({
  selector: 'app-client-form',
  standalone: true,
  template: `<p>Client form — coming in phase 6</p>`
})
export class ClientFormComponent {}
